# python -m venv venv
# venv\scripts\activate
# pip3 install pytest-selenium
# pip install selenium==4.*
# https://chromedriver.chromium.org/downloads

# import time
# from selenium import webdriver
# from selenium.webdriver.common.by import By
#
# driver = webdriver.Chrome()
#
# driver.get('http://www.google.com/')
#
# time.sleep(5)
#
# search_box = driver.find_element(By.NAME, "q")  # Ищем поле ввода
#
# search_box.send_keys('QaLearning')  # Вводим запрос
#
# search_box.submit()
#
# time.sleep(10)
#
# driver.save_screenshot('result.png')  # Сохраняем скриншот нашего результата
#
# driver.quit()


# find_element(By.ID, value)
# find_element(By.CSS_SELECTOR, value)
# find_element(By.XPATH, value)
# find_element(By.NAME, value)
# find_element(By.TAG_NAME, value)
# find_element(By.CLASS_NAME, value)
# find_element(By.LINK_TEXT, value)
# find_element(By.PARTIAL_LINK_TEXT, value)

# .click() — осуществляет клик левой кнопкой мыши в центр элемента
# .clear() — метод влияет только на поля ввода input и textarea и стирает весь текст внутри поля
# .send_keys(“text”) — вводит заданную строку в текстовое поле
# .submit() — инициирует отправку формы (аналогично нажатию Enter при выбранном поле ввода внутри формы).

# try:
#   Наш скрипт
#
# finally:
#
#     browser.quit()


